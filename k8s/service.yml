apiVersion: v1
kind: Service

metadata:
  name: task-manager-svc

spec:
  selector:
    app: task-manager-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort:   30007
  type: NodePort


